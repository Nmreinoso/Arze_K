import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://Nmreinoso.github.io',
  base: '/Arze_K',
  trailingSlash: 'always', // This helps GitHub Pages find the right folders
});
